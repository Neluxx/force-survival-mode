name: Force Survival Mode
version: 0.1.0

pipeline:
  - beet.contrib.render

meta:
  render:
    data_pack:
      - "**"

data_pack:
  description: {"text":"Force Survival Mode\n1.13 - 26.1", "color":"gray"}
  pack_format: 4
  min_format: 4
  max_format: 101
  supported_formats:
    min_inclusive: 4
    max_inclusive: 101
  load: src/datapack
  overlays:
    - directory: v1_21
      min_format: 48
      max_format: 101
      formats:
        min_inclusive: 48
        max_inclusive: 101

output: build
