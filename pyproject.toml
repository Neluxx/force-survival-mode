[project]
name = "force-survival-mode"
version = "0.2.1"
description = "A datapack that forces players into survival mode."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.115.0",
]
